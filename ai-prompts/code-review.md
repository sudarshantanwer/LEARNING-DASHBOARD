# Code Review Prompts (examples)

> “Review `server/src/controllers/taskController.ts` for consistent error handling: Zod vs domain errors vs 404s.”

> “Review `client/src/services/api.ts` for parsing edge cases (empty body) and user-facing error messages.”

> “Identify missing accessibility issues in `TaskForm` and suggest fixes without adding new dependencies.”
