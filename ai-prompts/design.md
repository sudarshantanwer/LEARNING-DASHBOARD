# Design Prompts (examples)

> “Design a modern SaaS dashboard UI using Tailwind: neutral slate surfaces, indigo primary, emerald success, compact cards, responsive tables that collapse gracefully on mobile.”

> “Specify UI states: loading spinner, empty dashed panel, error alert with retry, toast for success. Keep components reusable.”

> “Choose routing: `/`, `/tasks`, `/tasks/new`, `/tasks/:id`, `/tasks/:id/edit`, plus not-found handling inside the main layout.”
