# Testing Prompts (examples)

> “Write Supertest integration tests that set `TEST_DATA_DIR` to a temp directory containing `users.json` + `tasks.json`, then assert CRUD + dashboard stats.”

> “Write React Testing Library tests that mock `fetch` for `/api/dashboard`, `/api/tasks`, and `/api/users` to validate dashboard rendering and list filtering behavior.”

> “Configure Vitest coverage thresholds; if client coverage includes unrelated config files, adjust `coverage.include` / `all` settings.”
