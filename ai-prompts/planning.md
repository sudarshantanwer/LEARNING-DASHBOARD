# Planning Prompts (examples)

> “I’m building an assessment app called AI Learning Dashboard. Generate a monorepo plan with `client/` + `server/`, JSON persistence in `database/tasks.json` + `database/users.json`, Express routes exactly as specified, and a Vite React UI using TanStack Query + Tailwind. List milestones and risks.”

> “Define a repository/service split for Express + TS where repositories only do JSON IO and services enforce owner validation + dashboard aggregation.”

> “Propose a CI pipeline (GitHub Actions) that runs install, lint, build, and tests for npm workspaces.”
