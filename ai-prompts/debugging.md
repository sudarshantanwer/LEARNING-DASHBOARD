# Debugging Prompts (examples)

> “`tsc --noEmit` fails because tests are outside `rootDir`. Fix server tsconfig without breaking Vitest.”

> “Vitest prints React Router warnings about unmatched routes in tests—show how to extend `MemoryRouter` routes to include navigation targets.”

> “Docker compose: nginx proxies `/api` to the API container but requests 404—verify `proxy_pass` trailing slashes and Express mount path.”
