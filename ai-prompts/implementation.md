# Implementation Prompts (examples)

> “Implement `server/src/repositories/taskRepository.ts` using `fs/promises` read/write for `tasks.json`, and wire routes `/api/tasks` + `/api/dashboard`.”

> “Add Zod schemas for create/update/status patch. Return 400 with flattened issues on validation errors.”

> “Implement the client `tasksApi` using `fetch` helpers and TanStack Query hooks with cache invalidation on mutations.”

> “Add `GET /api/users` so the UI can render owner names without hardcoding seed users.”
